# Supabase Configuration
# Copy this file to .env.local and fill in your actual values from your Supabase project settings

# Supabase URL - found in your project settings under "Project URL"
NEXT_PUBLIC_SUPABASE_URL=

# Supabase Anon Key - found in your project settings under "Project API keys" -> "anon public"
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase Service Role Key - found in your project settings under "Project API keys" -> "service_role"
# ⚠️  NEVER commit this key to version control - it has admin privileges
SUPABASE_SERVICE_ROLE_KEY=

# Stripe Configuration
# ⚠️  NEVER commit these keys to version control
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Stripe Product Price IDs (public - safe to expose)
NEXT_PUBLIC_STRIPE_PRICE_MONTHLY=
NEXT_PUBLIC_STRIPE_PRICE_ANNUAL=

# OpenAI Configuration (for AI quiz generation)
# ⚠️  NEVER commit this key to version control
OPENAI_API_KEY=
# Optional: Custom OpenAI Base URL
OPENAI_BASE_URL=
# Optional: OpenAI Project ID
OPENAI_PROJECT_ID=

# Admin API Key for system operations (quota reset, etc.)
# ⚠️  NEVER commit this key to version control
ADMIN_API_KEY=

# Skolverket Syllabus API Configuration
# Feature flag to enable/disable Skolverket integration
FEATURE_SYLLABUS=true
# Base URL for Skolverket Syllabus API
SYLLABUS_BASE_URL=https://api.skolverket.se/syllabus
# Refresh schedule for curriculum data (used by GitHub Actions)
SYLLABUS_REFRESH_CRON=weekly

# Ably Configuration (for Live Quiz real-time features)
# ⚠️  NEVER commit the root API key to version control
ABLY_API_KEY=xxxx:yyyy
# Server API key for health checks
ABLY_SERVER_API_KEY=xxxx:yyyy
ABLY_TOKEN_TTL_SECONDS=3600

# Read-only key for public displays (optional - can be exposed in frontend)
NEXT_PUBLIC_ABLY_SUBSCRIBE_KEY=

# Feature flag for Live Quiz real-time features
NEXT_PUBLIC_FEATURE_LIVE_QUIZ=true